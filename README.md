# EaseProxy
this is repository of the project of de extencion for browser

## How to Test
Clone the repository using:
```bash
git clone https://github.com/gabriel1003/EaseProxy.git
```

Go to the folder:
```bash
cd EaseProxy
```

And run it with your browser; for example, using Firefox:
```bash
firefox index.html
```
